import { fetchPhotographers, fetchMedia, getPhotographerIdFromUrl, getPhotographerById } from './fetch.js';

export async function countLikes(params) {
    const likes = document.createElement('article');
    
}